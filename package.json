{
  "name": "mix-dashboard-exporter",
  "version": "1.0.0",
  "description": "Export Firebase Firestore data to JSON for Mix Dashboard",
  "main": "export-firebase-data.js",
  "scripts": {
    "export": "node export-firebase-data.js",
    "test": "jest"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "playwright": "^1.57.0"
  },
  "type": "commonjs",
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "jest-extended": "^7.0.0"
  }
}
